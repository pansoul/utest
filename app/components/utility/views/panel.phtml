<?
use UTest\Kernel\User\User;

if (!User::isAuth()) {
    return;
}
?>

<div class="user-panel">
    <a href="/<?=User::user()->getRole()?>/profile" class="user-name"><?=User::user()->getName()?></a>
    <a href="?logout=Y" class="user-logout">Выйти</a>
</div>