<?
use UTest\Kernel\Form;

$a = $data['answer_list'];
$r = $data['user_answer'];
?>

<ul class="noliststyle test-variant-list">
    <?foreach ($a as $id => $item):?>
        <li>
            <?=Form::radio("right", $id, false, "a-{$id}", $id == $r)?>
            <label for="a-<?=$id?>"><?=$item['title']?></label>
        </li>
    <?endforeach;?>
</ul>