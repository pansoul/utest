<?
use UTest\Kernel\Form;
use UTest\Kernel\Test\Passage;

if ($this->hasErrors(ERROR_ELEMENT_NOT_FOUND)) {
    echo Form::error($this->getErrors(ERROR_ELEMENT_NOT_FOUND));
    return;
}

if ($this->hasErrors()) {
    echo Form::error($this->getErrors(), true);
}
?>

<div class="bs-callout bs-callout-warning">
    <p>
        При назначении пересдачи студенту со статусом теста <span class="label label-info"><?= Passage::getTestStatuses(Passage::STATUS_IN_PROCESS) ?></span> весь его текущий прогресс сбросится.
    </p>
</div>

<form class="form-horizontal" method="post" action="">
    <div class="form-group">
        <label class="col-lg-3 control-label">Назначить пересдачу:</label>
        <div class="col-lg-9">
            <?=Form::checkbox("set_retake", 1)?>
        </div>
    </div>
    <div class="form-group">
        <div class="col-lg-offset-3 col-lg-9">
            <?=Form::button('Применить', 'btn btn-default')?>
        </div>
    </div>
    <?=Form::input('hidden', 'a', 'Y')?>
</form>