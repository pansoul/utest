<?
$v = $arResult['data'];

UAppBuilder::setH("&laquo;{$v['title']}&raquo;");
UAppBuilder::addBreadcrumb ('Пересдача теста', USite::getUrl());

if(empty($v)) {
    echo 'Группа не найдена';
    return;
}
?>

<div class="bs-callout bs-callout-warning">
    <p>
        Внимание! Пересдача применится только тем студентам, кто на данный момент уже прошёл выбранный тест
    </p>
</div>

<form class="form-horizontal" method="post" action="">
    <div class="form-group">
        <label class="col-lg-2 control-label">
            Назначить пересдачу:  
            <span class="help-block">После указания данной опции всем студентам выбранной группы будет возможность пройти тест повторно.</span>
        </label>
        <div class="col-lg-10">
            <?=UForm::checkbox("set_retake", 1)?>
        </div>
    </div>
    <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
            <?=UForm::button('Применить', 'btn btn-default')?>
        </div>
    </div>
    <?=UForm::input('hidden', 'a', 'Y')?>
</form>