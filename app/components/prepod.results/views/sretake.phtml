<?
$v = $arResult['data'];

UAppBuilder::setH("{$v['u_last_name']} {$v['u_name']} {$v['u_surname']}");
UAppBuilder::addBreadcrumb ('Пересдача теста', USite::getUrl());

if(empty($v)) {
    echo 'Пройденный тест для данного студента не найден';
    return;
}
?>

<form class="form-horizontal" method="post" action="">
    <div class="form-group">
        <label class="col-lg-2 control-label">
            Назначить пересдачу:  
            <span class="help-block">После указания данной опции студент сможет пройти тест повторно.</span>
        </label>
        <div class="col-lg-10">
            <?=UForm::checkbox("set_retake", 1)?>
        </div>
    </div>
    <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
            <?=UForm::button('Применить', 'btn btn-default')?>
        </div>
    </div>
    <?=UForm::input('hidden', 'a', 'Y')?>
</form>